version: 2
projects:
- autoplan:
    when_modified: ["*.tf", "../modules/**.tf"]
    enabled: true
  apply_requirements: [approved]
  workflow: testF5Workflow
workflows:
  testF5Workflow:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply
