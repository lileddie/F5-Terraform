version: 2
projects:
- name: testF5
  dir: /terraform/F5
  autoplan:
    when_modified: ["*.tf", "../modules/**.tf"]
    enabled: true
  apply_requirements: ["approved"]
workflows:
  testF5Workflow:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply
