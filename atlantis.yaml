version: 2
projects:
- name: root
  dir: .
    autoplan:
      enabled: true
  workflow: stdF5Workflow
workflows:
  stdF5Workflow:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply
